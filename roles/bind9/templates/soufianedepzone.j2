;
; BIND data file for local loopback interface
;
$TTL	604800
@	IN	SOA	{{ nommachine }}.{{ domaine }}. root.{{ domaine }}. (
			      2		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
;
@	IN	NS	{{ nommachine }}.{{ domaine }}.
{{ nommachine }}	IN	A	192.168.91.143
www	IN	CNAME	{{ nommachine }}.{{ domaine }}.
{{ domaine }}.	IN	MX	10	{{ nommachine }}.{{ domaine }}.
